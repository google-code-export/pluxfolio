#summary Функции к реализации в будущих версиях системы
#labels Deprecated,Phase-Requirements,Phase-Design,Phase-Implementation,private

  # Скрытая карта сайта для поисковых роботов
  # Оптимизация работы системы:
    * объединение соседних echo
    * оптимизация работы системы при построении галереи (вынос основных действий в функции plx.util)
    * замена {{{ if (...) {...}; if (...) {}; }}} на {{{ if (...) {...} else if (...) {} }}}
    * удаление неиспользуемых переменных из памяти для высвобождения RAM
    * замена {{{ $row[id] }}} на {{{ $row['id'] }}}
    * изменение алгоритма подсчета работ в галереях (за основу взять remove_tb.php)
    * *просмотреть http://habrahabr.ru/blogs/php/112474/*